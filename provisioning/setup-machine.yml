- name: a playbook to setup my local machine with my basic customizations
  hosts: dev-machine
  tasks:
  - name: Do something
    command: echo "blah"
    register: output
  - debug: msg="{{output.stdout}}"